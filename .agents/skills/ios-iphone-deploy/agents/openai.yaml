interface:
  display_name: "iPhone Deploy"
  short_description: "Build and install the iOS app on iPhone"
  default_prompt: "Use $ios-iphone-deploy to build and deploy the current Dubai Missile Command iOS app to my connected iPhone."

policy:
  allow_implicit_invocation: true
